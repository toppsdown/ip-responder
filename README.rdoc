== README

This repo is a simple ip responding app for upload to heroku

It will return the currently given IP address as well as a
link to a table of all the ip addresses that have visited and the
number of times they have done so

You can see the app at: https://ip-responder.herokuapp.com/
